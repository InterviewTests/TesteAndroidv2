<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="android:textColorHint">@color/grey</item>
        <item name="android:textSize">@dimen/ts</item>
        <item name="android:windowBackground">@color/offWhite</item>
<!--        <item name="android:navigationBarColor">@color/offWhite</item>-->
        <item name="android:statusBarColor">@color/colorAccent</item>
    </style>

    <style name="LoginTheme" parent="AppTheme">
        <item name="android:statusBarColor">@color/offWhite</item>
    </style>

</resources>