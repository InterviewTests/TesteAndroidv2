<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.develop.tcs_bank">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
            android:name=".presentation.main.TcsApplication"
            android:allowBackup="true"
            android:icon="@drawable/ico_app"
            android:label="@string/app_name"
            android:roundIcon="@drawable/ico_app"
            android:supportsRtl="true"
            android:theme="@style/AppTheme.NoActionBarStatusBar">
        <activity android:name=".presentation.login.NavigationRegistration" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>